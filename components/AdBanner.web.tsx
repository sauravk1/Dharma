import React from 'react';
import { View } from 'react-native';

// This file is used ONLY on the web.
// We return an empty view because Ads don't work on the web.
export const AdBanner = () => {
  return <View />;
};
